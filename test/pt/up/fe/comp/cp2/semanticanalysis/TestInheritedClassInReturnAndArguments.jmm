import Animal;

class Cao extends Animal {

    public int test(){
        Cao cao;
        Animal animal;


        cao = new Cao();
        animal = new Animal();

        animal = cao;

        animal = this.foo(cao);
        animal = this.bar(cao);

        return 0;
    }

    public Animal foo(Animal animal) {
        return animal;
    }

    public Animal bar(Cao cao) {
        return cao;
    }
}