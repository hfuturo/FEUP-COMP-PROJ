import length;

class length {

    int length;

    public int foo() {
        int length;
        return 0;
    }

    public length length(length length) {
        length a;
        a = length;
        return length;
    }

}